/**
 * Auto-fit target extent (largest axis in scene units after bbox fit).
 * Offsets in `garmentView.ts` scale with the matching constant.
 */
export const FIT_MAX_COMPACT = 1.5
/** Studio viewer — lower = smaller garment on screen for all products */
export const FIT_MAX_STUDIO = 1.25
